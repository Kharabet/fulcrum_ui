{
  "name": "root",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "cross-env": "^5.2.0",
    "lerna": "^3.14.1",
    "shx": "^0.3.2"
  },
  "scripts": {
    "clean-all": "shx rm -rf ./node_modules && shx rm -rf ./packages/**/node_modules",
    "build-all": "yarn run build-fulcrum && yarn run build-torque",
    "build-fulcrum": "",
    "build-torque": ""
  }
}
